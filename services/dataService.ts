// This service has been deprecated. User-specific data initialization is now handled
// by the `useLocalStorage` hook with default values when a new user logs in.
export const seedInitialData = () => {
  // Deprecated: Do not use.
};
